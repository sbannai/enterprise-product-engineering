import test from "node:test";import assert from "node:assert/strict";import{STATES,RecordRepository,AuthorizationError,ValidationError,validateMaterialChange,audit,controlledException,report}from"../src/domain.js";
const A={tenantId:"tenant-a",principalId:"user-a",permissions:["record:create","record:read","record:transition","report:read"]},B={tenantId:"tenant-b",principalId:"user-b",permissions:["record:create","record:read","record:transition","report:read"]};
test("REQ-48001 lifecycle/history",()=>{const r=new RecordRepository(),x=r.create(A,{reference:"R-480",category:"neutral"}),y=r.transition(A,x.id,STATES.ACTIVE,"activate");assert.equal(y.version,2);assert.equal(y.history.length,2)});
test("REQ-48002 authorization/cross-tenant",()=>{const r=new RecordRepository(),x=r.create(A,{reference:"R-1",category:"neutral"});assert.throws(()=>r.list({tenantId:"tenant-a",permissions:[]}),AuthorizationError);assert.equal(r.list(B).length,0);assert.equal(x.tenantId,"tenant-a")});
test("REQ-48003 mandatory validation",()=>{const r=new RecordRepository(),x=r.create(A,{reference:"R-2",category:"neutral"});assert.throws(()=>validateMaterialChange(x,{approvalConfirmed:false,preconditionsMet:true}),ValidationError);assert.equal(validateMaterialChange(x,{approvalConfirmed:true,preconditionsMet:true}),true);assert.throws(()=>r.transition(A,x.id,STATES.COMPLETED,"skip"),ValidationError)});
test("REQ-48004 audit evidence",()=>{const x=audit("RECORD_ACTIVATED","user-a","resource-480");assert.equal(x.actor,"user-a");assert.ok(x.at)});
test("REQ-48005 controlled exception",()=>{const x=controlledException("PRECONDITION_FAILED","Required condition missing",{field:"approval"});assert.equal(x.status,"OPEN")});
test("REQ-48006 governed reporting",()=>{const r=new RecordRepository();r.create(A,{reference:"A",category:"neutral"});r.create(B,{reference:"B",category:"neutral"});assert.equal(report(A,r).total,1);assert.throws(()=>report({tenantId:"tenant-a",permissions:[]},r),AuthorizationError)});
